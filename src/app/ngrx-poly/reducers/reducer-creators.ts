import { depthOneReducerCreator } from './depth-one-reducer'
import { depthTwoReducerCreator } from './depth-two-reducer'

export const ReducerCreators = {
  createDepthOneReducer: depthOneReducerCreator,
  createDepthTwoReducer: depthTwoReducerCreator,
}
